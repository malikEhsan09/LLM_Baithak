[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8001}"

[[services]]
type = "web"
name = "LLM Baithak Backend"
envVars = ["PORT", "OPENROUTER_API_KEY"]
